{
  "$schema": "../schema/tool-schema.json",
  "id": "list_skills",
  "name": "list_skills",
  "description": "List all available development skills with descriptions and IDs",
  "input_schema": {
    "type": "object",
    "properties": {
      "tags": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Optional: filter by tags (clean-code, testing, etc.)"
      }
    },
    "required": []
  }
}
